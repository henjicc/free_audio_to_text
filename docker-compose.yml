version: '3'

services:
  free_audio_to_text:
    build: .
    container_name: free_audio_to_text
    restart: unless-stopped
    ports:
      - "8000:8000"
    volumes:
      - ./config:/app/config
      - ./logs:/app/logs
      - ./downloads_temp:/app/downloads_temp
    environment:
      - TZ=Asia/Shanghai 